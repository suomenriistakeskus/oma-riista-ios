import Foundation

enum AuthenticationUiConstants {

    static let activeTabBackgroundColor: UIColor = .black.withAlphaComponent(0.6)
    static let inactiveTabBackgroundColor = UIColor.applicationColor(Primary)
    static let contentBackgroundColor: UIColor = .black.withAlphaComponent(0.7)

    static let horizontalMarginsToScreen: CGFloat = 16
}
